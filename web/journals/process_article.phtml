<?
 # include "header.inc";
    include "connect.inc";
  // Init the date and time
  $date = date("Y-m-d");
  $time = time();

  if ( $username == "" ) {
    error("This username is not set");
    exit;
  }
   // snafu saves the day with this one:

/*
17:38:39 chrissnafu: if (substr($url,0,7) != 'http://' && substr($url,0,7) !=
'mailto:')
17:38:40 chrissnafu: {
17:39:15 chrissnafu: if (strstr($url,'@')) { $url = "mailto:$url"; }
17:39:39 chrissnafu: else { $url = "http://$url"; }
17:39:40 chrissnafu: }
17:39:48 chrissnafu: slightly smarter
17:42:09 chrissnafu: also
17:42:39 chrissnafu: foreach ($image_arr as $index => $img_url)
{
17:43:37 chrissnafu: echo "<a>
";
}
17:43:45 chrissnafu: oh, fucking aim.
17:43:53 chrissnafu: nevermind
17:43:56 robotec9: get on irc



*/





 
  // Work out what subjects
  $res = mysql("swinney", "SELECT MAX(category_id) FROM categories");
  $maxid = mysql_result($res, 0, 0);
  $subject_value = 0;
  for ( $i = 1 ; $i <= $maxid ; $i++ ) {
    $str = "cat".$i;
    if ( $$str == "on" ) {
      $val = pow(2, $i-1);
      $subject_value += $val;
    }
  }


  // Insert into the database
  $res = mysql	 ("swinney", "INSERT INTO pending_articles
	 VALUES ( 
	'NULL', 
	'$username', 
        '$web',
	'$date', 
	'$time', 
	'1', 
	'".addslashes($keywords)."',
	'".addslashes($title)."',
	'".addslashes($blurb)."', 
	'$category',
	'$image'
	)");

  if ( $res == false ) { 
    echo("Bullocks!");
    exit;
  }

  // Create a file in  /pending_articles
  // if you don't have the auto_increment integer, then mysql_insert_id is not going to work
  $article_id = mysql_insert_id();  
  # echo "$article_id<br>";
  $filename = "pending/".mysql_insert_id().".txt";
  $fp = fopen($filename, "w");
  fwrite($fp, stripslashes($content) );
  fclose($fp);

  // Now get the images



# include "submit_image.phtml";

 mail("article@swinney.org", "$title by $username", "$blurb\n $content");
 mail("aswinney@swinney.org", "$title by $username", "$blurb\n $content");
 mail("steve@saturn5.com", "$title by $username", "$blurb\n $content");
 header("Location:submit_image.phtml?article_id=$article_id" ); 

?>








































